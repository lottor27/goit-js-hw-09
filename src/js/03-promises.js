function createPromise(position, delay) {
  const shouldResolve = Math.random() > 0.3;
  if (shouldResolve) {
    // Fulfill
  } else {
    // Reject
  }
}


function createPromise(position, delay) {
  console.log("Helloy");
}

console.log('Helloy');